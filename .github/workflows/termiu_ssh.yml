name: Termius SSH Session

on:
  workflow_dispatch:

jobs:
  ssh-session:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Start SSH Session
        uses: mxschmitt/action-tmate@v3
        with:
          limit-access-to-actor: false
